// index.js
// 获取应用实例
const app = getApp()

Page({
  data: {
  },
  onLoad(params) {
    this.setData({
      title: "首页"
    }, () => {
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
    this.setData({
      [Math.random()]: Math.random()
    })
  },
})
